<nav class="navbar bg-primary text-secondary">
  <div class="custom-nav-container">
    <button
      class="nav-buttons bg-primary text-secondary"
      type="button"
      aria-label="Toggle navigation"
    >
      <i class="material-icons outlined">menu</i>
    </button>
      <h2 class="custom-navbar-header"><%= title %></h2>
      <% if user_signed_in? %>
      
      <button
        class="nav-buttons bg-primary"
        type="button"
        aria-label="Logout button"
      >
        <%= link_to destroy_user_session_path, data: {turbo_method: :delete },
        id: 'nav-logout-link' do %>
          <i class="material-icons outlined">logout</i>
        <% end %>
      </button>
      <% else %>  
      
      <button
        class="nav-buttons bg-primary"
        type="button"
        aria-label="Login button"
      >
      <%= link_to new_user_session_path,id: 'nav-login-link'  do %>
        <i class="material-icons outlined">login</i>
        <% end %>
      </button>
      <% end %>
  </div>
</nav>
